spring:
  datasource:
    url: jdbc:mysql://proxysql:6033/racs
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 5000
  http-port: 3443
  ssl:
    enabled: false
